#include <math.h>
#include "intersections.h"
#include <stdio.h>

//if there is no intersection, this function should return 0
//Otherwise, populate 'intersection' with the point of intersection and return 1
int ray_disk_intersection(struct ray observer, struct disk obj, double *intersection) {
  //Question 3: Revise this function so it correctly computes an intersection
  return 0;
}

//if there is no intersection, this function should return 0
//Otherwise, populate 'intersection' with the point of intersection and return 1
int ray_cylinder_intersection(struct ray observer, struct cylinder obj, double *intersection) {
  //Question 5: Revise this function so it correctly computes an intersection
  return 0;
}

int ray_cone_intersection(struct ray observer, struct cone obj, double *intersection) {
  //Question 7: Revise this function so it correctly computes an intersection
  return 0;
}
